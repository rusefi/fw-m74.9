name: Build Unit Tests

on:
  schedule:
    - cron: '0 1 * * *'
  push:
  #  pull_request:
  workflow_dispatch:

jobs:
  build-firmware:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '11'

      - name: Test Java Compiler
        shell: bash
        run: javac -version

      - name: Test M74.9 memory layout and bootloader contract
        run: python3 -m unittest discover -s tests -p 'test_memory_contract.py' -v

      - name: Test and package M74.9 Java UI
        shell: bash
        run: bash bin/java-ui.sh --console=plain :custom-java-ui:test :ui:shadowJar

      - name: Print Compiler version
        shell: bash
        run: gcc -v

      - name: Set Env Variables
        id: set-env-variables
        shell: bash
        run: |
          : Set Env Variables
          ABSOLUTE_BOARD_DIR=${{github.workspace}}
          echo "ABSOLUTE_BOARD_DIR=$ABSOLUTE_BOARD_DIR" >> $GITHUB_ENV
          echo "META_OUTPUT_ROOT_FOLDER=$(realpath --relative-to=ext/rusefi/firmware generated)/" >> $GITHUB_ENV

      - name: Compile and execute Unit Tests
        shell: bash
        run: |
          : Compile and execute Unit Tests
          bash bin/compile_and_run_unit_tests.sh    
